allprojects {
    afterEvaluate {
        neoForge.runs.named('clientDev') {
            sourceSet.set(sourceSets.editor)
            gameDirectory.set(file('run/ability-vfx'))
            systemProperty 'academy.mindDestructionSmoke', 'true'
            programArguments.addAll('--width', '1280', '--height', '720')
        }
    }
}
