# 当前运行时验收清单

本文汇总能力移植、熟练度、Mentalout、UI 和旧物品迁移中仍需人工或多人环境确认的项目。历史文档中的“测试通过”只表示当时提交的结果；完成本清单时应以当前分支重新验证。

状态使用：`待执行`、`通过`、`失败`、`不适用`。填写结果时同时记录日期、环境和相关问题编号。

## 自动化门禁

| 项目 | 状态 | 结果记录 |
| --- | --- | --- |
| `./gradlew test -DisDev=true` | 待执行 |  |
| `./gradlew build -DisDev=true` | 待执行 |  |
| `./gradlew build -DisDev=false` | 待执行 |  |
| Mentalout GameTest 与已知时序基线 | 待执行 |  |
| 专用服务端启动，无客户端类加载错误 | 待执行 |  |

## 客户端与基础系统

| 场景 | 状态 | 验收重点 |
| --- | --- | --- |
| 开发客户端启动 | 待执行 | 8 类别、94 技能注册；资源和声音重载；无 Academy Mixin apply 错误 |
| 数据终端按键设置 | 待执行 | 修改、重置、持久化；按下/松开绑定；键盘与鼠标修饰键 |
| 能力开发机与 V 键 HUD | 待执行 | 技能说明、依赖连线、当前绑定、未学习分支和分类切换 |
| UI 布局覆盖与发布 | 待执行 | bundled/override 回退、控件绑定、备份和 HUD 默认锚点 |
| 双玩家配置同步 | 待执行 | 服务端策略、熟练度档位、技能禁用提示与状态同步 |

## 能力分类

| 分类 | 状态 | 必测场景 |
| --- | --- | --- |
| Level0 | 待执行 | 学习链、CP/迭代/堆栈修正、耐力与体术属性、过载恢复 |
| Aeromanip | 待执行 | 推进和制动、友军保护、领域预算、飞行租约、死亡/换维度清理 |
| Accelerator | 待执行 | 伤害/投射物/效果反射、过滤列表、所有翼形态互斥、CP 不足和退出清理 |
| Electromaster | 待执行 | Railgun 弹药、充能目标、护盾溢出和冷却、雷击技能、双玩家行为 |
| Meltdowner | 待执行 | 射束延迟与持续状态、方块权限、辐射归属、自动巡航目标过滤 |
| Teleport | 待执行 | 服务端落点、边界和未加载区块；区域搬移的方块实体、流体、计划刻、载具与回滚 |
| Darkmatter | 待执行 | 装备、甲虫所有权、六翼租约、修复/复制边界；可选 PSI 集成仍不在核心门禁内 |
| Mentalout | 待执行 | Mob/玩家控制、挣扎、感知过滤、友军继承、程序四槽、断线/死亡/换维度清理 |

## 旧物品迁移

| 场景 | 状态 | 验收重点 |
| --- | --- | --- |
| 已迁移材料和风机显示屏 | 待执行 | 创造页、双语名称、模型、配方与数据包加载 |
| 虚相位探矿器 | 待执行 | 仅扫描已加载区块、网络同步、手持/GUI 变换和立体标记渲染 |
| 未迁移物品 | 待执行 | 未注册引用、配方和数据生成均不得残留 |

## 完成条件

- 所有适用行均记录为 `通过`，或关联明确的缺陷/延期决定。
- 双玩家与专用服务端结果不能由单机客户端结果替代。
- 可见 UI、粒子、射束、翼和物品模型应附截图或短录屏。
- 关闭本清单前重新运行自动化门禁，并在变更说明中列出命令与手工检查结果。
